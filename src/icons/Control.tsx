import { createIcon } from "../create-icon";

export const Control = createIcon("control", [
  [
    "path",
    {
      d: "M7 15L16 6L25 15",
      stroke: "currentColor"
    }
  ]
]);

export default Control;
