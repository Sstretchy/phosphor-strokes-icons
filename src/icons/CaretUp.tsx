import { createIcon } from "../create-icon";

export const CaretUp = createIcon("caret-up", [
  [
    "path",
    {
      d: "M6 20L16 10L26 20",
      stroke: "currentColor"
    }
  ]
]);

export default CaretUp;
