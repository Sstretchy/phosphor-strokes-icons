import { createIcon } from "../create-icon";

export const Check = createIcon("check", [
  [
    "path",
    {
      d: "M5 18L12 25L28 9",
      stroke: "currentColor"
    }
  ]
]);

export default Check;
